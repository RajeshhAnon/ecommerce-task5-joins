# Task 5 – SQL Joins on E-commerce Database

## ✅ Objective
To practice `INNER`, `LEFT`, simulated `RIGHT`, and simulated `FULL OUTER JOIN` using SQLite.

## 🧠 Queries Covered
- INNER JOIN (Users + Orders)
- LEFT JOIN (Users with/without Orders)
- Simulated RIGHT JOIN
- Simulated FULL OUTER JOIN
- Multi-table JOIN: Orders → OrderItems → Products

## 📂 Files
- `task5_joins.sql`: All join queries
